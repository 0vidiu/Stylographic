/**
 * A kind of CSS console :-)
 */
body:before {
    content: "";
    position: fixed;
    bottom: 0;
    left: 0;
    z-index: 100;
    width: 100%;
    padding: rem(12);
    background-color: rgba(black, 0.6);
    color: white;
    font-family: "Courier New", monospace;
    font-size: rem(14);
}


console($log);
